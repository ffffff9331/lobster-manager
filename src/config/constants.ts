/** OpenClaw Gateway 默认端口 */
export const GATEWAY_DEFAULT_PORT = 18789;

/** 本机 Gateway 默认 base URL */
export const GATEWAY_DEFAULT_URL = `http://127.0.0.1:${GATEWAY_DEFAULT_PORT}/`;

/** 健康检查超时（毫秒） */
export const HEALTH_CHECK_TIMEOUT_MS = 3000;
